### Example environment variables for Supabase
# Copy this file to .env and fill values (do NOT commit real secrets)

# Supabase service (server-side) credentials
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Auth secret (long random string) used by Auth.js for signing cookies / JWT
AUTH_SECRET=some-long-random-string

# Optional: NEXTAUTH_URL or base URL if needed (dev default is http://127.0.0.1:5173)
NEXTAUTH_URL=http://127.0.0.1:5173

# Notes:
# - Create a Supabase project and add the required tables: Customers, Products, Transactions_Orders, Transactions_Balance
# - DO NOT commit real secrets into git. Use environment variables in your deployment.
