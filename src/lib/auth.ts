// Minimal auth stub to satisfy imports during typechecking.
// Replace this with your real authOptions implementation when available.

// Avoid importing types from @auth/core to keep this file minimal and
// prevent type errors during svelte-check. Use `any` for now.
export const authOptions: any = {
  // Intentionally minimal. Add providers, callbacks, options as needed.
};
